# IMPORTANTE: Este arquivo é apenas um exemplo
# Copie este arquivo para .env.production e preencha com valores reais
# NUNCA commite o arquivo .env.production no Git

# =====================================
# VARIÁVEIS DO BACKEND
# =====================================

# Ambiente de execução
NODE_ENV=production

# Porta do servidor backend
PORT=3001

# Secret para assinatura de JWT (GERE UM VALOR SEGURO!)
# Gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=SUBSTITUA_POR_UM_SECRET_SEGURO_GERADO_64_CARACTERES_MINIMO

# Origem CORS permitida (domínio do frontend)
CORS_ORIGIN=https://clinica.123atendi.com.br

# =====================================
# VARIÁVEIS DO FRONTEND
# =====================================

# URL da API backend (usada durante o build do React)
REACT_APP_API_URL=https://clinica.123atendi.com.br/api

# =====================================
# VARIÁVEIS DO DOCKER COMPOSE
# =====================================

# Mesma secret do JWT acima (será injetada no container)
JWT_SECRET=SUBSTITUA_POR_UM_SECRET_SEGURO_GERADO_64_CARACTERES_MINIMO
